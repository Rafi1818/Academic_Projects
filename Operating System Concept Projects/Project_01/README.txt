1. Once the program is compiled it will ask for an input.
2. If the input contains anything other than a valid integer or the string 'quit' it will show an invalid input error and ask the user again for a valid input. 
3. The program will only terminate if you enter 'quit'. Otherwise it will be asking for a valid input over and over again.
